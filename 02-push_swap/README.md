*This project has been created as part of the 42 curriculum by tsellak.*

# push_swap

## Description

The **push_swap** project is a very simple and highly effective algorithm project: data must be sorted. You have at your disposal a set of int values, 2 stacks, and a set of instructions to manipulate both stacks.

The goal is to write a program in C called `push_swap` which calculates and displays on the standard output the smallest program instructions, using the Push Swap instruction language, that sorts the integers received as arguments.

## Instructions

### Compilation

To compile the project, run the following command in the project root:

```bash
make
```

This will create the `push_swap` executable.

### Execution

Run the program with a list of integers as arguments:

```bash
./push_swap 2 1 3 6 5 8
```

The program will output the list of operations needed to sort the stack.

### cleanup

To remove object files:
```bash
make clean
```

To remove object files and the executable:
```bash
make fclean
```

## Bonus Part

The project includes a bonus part: a checker program that validates whether the list of instructions generated by `push_swap` actually sorts the stack.

### Checker Compilation

To compile the bonus part, run:

```bash
make bonus
```

This will create the `checker` executable.

### Checker Execution

The `checker` program takes the same list of integers as arguments. It then waits for instructions on the standard input. You can test it by piping the output of `push_swap` into `checker`:

```bash
ARG="4 67 3 87 23"; ./push_swap $ARG | ./checker $ARG
```

*   If the instructions sort the stack correctly, the program outputs `OK`.
*   If the stack is not sorted or the instructions are incorrect, it outputs `KO`.
*   If there is an error in the arguments or instructions, it outputs `Error`.

## Resources

### References
*   [Sorting Algorithms - GeeksforGeeks](https://www.geeksforgeeks.org/sorting-algorithms/)
*   [Big O Notation - Wikipedia](https://en.wikipedia.org/wiki/Big_O_notation)

### AI Usage

Artificial Intelligence was used in the development of this project for the following tasks:

*   **Concept Explanation**: AI explained core concepts of the project including stack operations and Big O complexity to ensure a solid theoretical foundation.
*   **Debugging**: AI assistant helped identify and fix bugs in `ft_split` and parsing logic (`parsing.c`, `parsing_utils.c`) to ensure robust input handling.
*   **Testing**: AI generated test suites and test cases to verify the correctness of swap, push, rotate, and reverse rotate operations.
*   **Documentation**: This README file was generated with the assistance of AI to strictly follow the project submission guidelines.
